"""
MiroFish ABM (Agent-Based Modeling) — Grandmaster Edition.

Mesa-based social simulation with hybrid agents, SQLite interaction ledger,
recommendation-curated feeds, GraphRAG entity retrieval, dynamic temporal
memory, ensemble trend prediction, and deep interaction chat interfaces
for emergent market sentiment analysis.

Grandmaster modules:
    graphrag          — NL query → subgraph retrieval → LLM context
    graph_visualizer  — D3.js JSON, Mermaid, adjacency matrix, temporal
    config_agent      — Auto-configure sim params from data richness
    trend_predictor   — Ensemble forecast T+5/T+10/T+30 with CI
    chat_interface    — Agent chat (in-character) + ReportAgent chat
"""
